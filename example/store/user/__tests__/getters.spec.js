import initState from '../state';

const context = {
  state: initState(),
};

/**
 * user getters
 */
describe('User getters', () => {
  
  beforeEach(() => {
    context.state = initState();
  });
});