import initState from '../state';

const mocks = {
  state: initState(),
};

/**
 * user getters
 */
describe('User getters', () => {
  
  beforeEach(() => {
    mocks.state = initState();
  });
});