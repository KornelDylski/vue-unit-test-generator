import initState from '../state';

const context = {
  state: initState(),  
};

/**
 * user mutations
 */
describe('User mutations', () => {
  
  beforeEach(() => {
    context.state = initState();
  });
});