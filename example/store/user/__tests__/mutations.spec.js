import initState from '../state';

const mocks = {
  state: initState(),  
};

/**
 * user mutations
 */
describe('User mutations', () => {
  
  beforeEach(() => {
    mocks.state = initState();
  });
});