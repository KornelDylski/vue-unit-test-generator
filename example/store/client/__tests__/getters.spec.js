import { state as initState } from '../index';

const mocks = {
  state: initState(),
};

/**
 * client getters
 */
describe('Client getters', () => {
  
  beforeEach(() => {
    mocks.state = initState();
  });
});